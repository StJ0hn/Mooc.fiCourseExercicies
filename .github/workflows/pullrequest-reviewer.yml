name: Mooc.fi Gemini Reviewer

on:
  pull_request:
    types: [opened, synchronize] # Roda quando abre o PR e quando você atualiza ele

jobs:
  review:
    runs-on: ubuntu-latest

    # Dá permissão para o bot comentar no seu PR
    permissions:
      pull-requests: write
      issues: write

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar o Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Instalar dependências
        run: pip install google-genai requests

      - name: Rodar a revisão da IA
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: python .github/scripts/reviewer.py
